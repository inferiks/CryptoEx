{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && cd crypto_backend && python manage.py collectstatic --noinput && python manage.py migrate"
  },
  "start": "gunicorn crypto_backend.wsgi"
}
