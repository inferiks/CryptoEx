{
  "build": [
    "pip install -r requirements.txt"
  ],
  "start": "cd crypto_backend && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn crypto_backend.wsgi:application"
}
